Workflow skill: $synthia-workflow 
(/home/dan/.codex/skills/synthia-workflow/SKILL.md)

## Codex Execution Directive

You are acting as the Synthia development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

Do not edit above this line:
#######################################################################
[STOP HERE]


[STOP HERE]


[TODO] Task 075: Phase 2C — Implement secure storage updates for Phase 2 state
  - Extend local persisted state to include Phase 2 metadata such as:
    - enabled provider/service selection
    - accepted capability declaration metadata
    - active governance version
    - governance sync timestamp
    - capability declaration timestamp
  - Add validation and migration-safe load behavior.

  * Comments, descriptions, rules:
    - Keep persisted state organized and versionable.
    - Do not overwrite trust-state semantics from Phase 1.
    - Maintain separation between identity/trust data and capability/governance data where practical.


[STOP HERE]


[TODO] Task 076: Phase 2D — Implement logging and diagnostics for capability activation flow
  - Add structured logging for:
    - post-trust activation path
    - provider/service selection
    - capability manifest creation
    - capability submission
    - governance sync
    - governance freshness changes
    - degraded/recovery transitions
  - Make logs useful for debugging Phase 2 activation failures.

  * Comments, descriptions, rules:
    - Logs must redact secrets and tokens.
    - Prefer stateful, compact logs over noisy chatter.
    - This task exists to keep later phases debuggable.


[TODO] Task 077: Phase 2D — Create Phase 2 validation and test checklist
  - Create a test checklist covering:
    - trusted restart path
    - provider/service selection persisted
    - capability manifest built correctly
    - capability declaration accepted
    - governance synced and stored
    - stale governance handling
    - trusted operational channel readiness
    - degraded and recovery behavior
  - Validate lifecycle state transitions for each scenario.

  * Comments, descriptions, rules:
    - This can begin as a manual/integration checklist.
    - Keep it aligned with the architecture docs and corrected contracts.
    - The goal is to stabilize node activation before prompt/provider execution phases begin.


[TODO] Task 078: Phase 2D — Finalize Phase 2 implementation review and handoff note
  - Review implemented Phase 2 behavior against the architecture/docs.
  - Document any deviations, follow-up fixes, or deferred edge cases.
  - Prepare a short handoff summary for the next phase:
    - prompt/service registration
    - prompt probation
    - execution gateway preparation

  * Comments, descriptions, rules:
    - Phase 2 is complete only when the node is trusted, capability-known, governance-synced, and operationally ready.
    - Do not begin prompt registration work inside this task.
    - This task cleanly closes Phase 2.

[TODO] Task 079: Define the UI state model for the AI Node dashboard
  - Create a canonical UI state model representing the node lifecycle and operational status.
  - The UI must support displaying:
    - lifecycle state
    - onboarding progress
    - runtime health
    - core connection info
    - capability summary
    - service status
  - Map UI states to backend lifecycle states:
    - unconfigured
    - bootstrap_connecting
    - registration_pending
    - pending_approval
    - trusted
    - capability_setup_pending
    - operational
    - degraded

  * Comments, descriptions, rules:
    - The UI must treat lifecycle state as the primary indicator of node status.
    - The dashboard should be able to render meaningful information even if some backend endpoints are temporarily unavailable.
    - Keep the state model aligned with the architecture documentation.


[TODO] Task 080: Add lifecycle status card to the dashboard
  - Implement a new "Lifecycle" card in the dashboard.
  - Display:
    - current lifecycle state
    - trust status
    - paired Core ID
    - pairing timestamp
    - governance status
  - Add visual state indicators (color-coded):
    - green: operational
    - yellow: pending states
    - orange: degraded
    - red: error

  * Comments, descriptions, rules:
    - This card becomes the main diagnostic surface for node state.
    - Lifecycle state should always be visible without navigating to other pages.
    - The lifecycle card must update dynamically if the backend state changes.


[TODO] Task 081: Enhance onboarding card with progress visualization
  - Replace the current static onboarding description with real progress indicators.
  - Show steps:
    - bootstrap discovery
    - registration
    - approval
    - trust activation
  - Each step should show one of:
    - pending
    - in progress
    - completed
    - failed
  - Add icons or badges to visualize progress.

  * Comments, descriptions, rules:
    - Operators must be able to see exactly where onboarding stopped if it fails.
    - Steps should map directly to Phase 1 onboarding states.
    - Avoid hardcoding; read onboarding state from backend.


[TODO] Task 082: Improve runtime health card with operational signals
  - Expand the Runtime card to show real operational status.
  - Include:
    - Core API connectivity
    - operational MQTT connectivity
    - governance freshness
    - last telemetry timestamp
    - node health state
  - Display connectivity indicators (connected / disconnected / degraded).

  * Comments, descriptions, rules:
    - Runtime health must reflect the current node condition.
    - Avoid exposing sensitive credentials.
    - Health indicators should refresh periodically.


[TODO] Task 083: Add Core connection information panel
  - Display current Core connection metadata:
    - Core ID
    - Core API endpoint
    - operational MQTT host and port
  - Show whether the node is currently connected to the Core.
  - Add a link or reference to Core onboarding/registration status.

  * Comments, descriptions, rules:
    - Operators often debug connection issues; this card must clearly show the Core endpoint.
    - Never display tokens or secrets.
    - This panel should appear only once the node is trusted.


[TODO] Task 084: Add capability summary card (Phase 2 readiness)
  - Create a new dashboard card showing node capabilities.
  - Display:
    - declared task families
    - enabled providers
    - capability declaration timestamp
    - governance policy version
  - Provide placeholders for future provider expansion.

  * Comments, descriptions, rules:
    - This card becomes active once Phase 2 capability declaration is complete.
    - The UI should gracefully handle cases where capability data is not yet available.
    - Do not include prompt-level data here.


[TODO] Task 085: Improve service management card with control actions
  - Expand the Service card to display:
    - backend service status
    - frontend service status
  - Add service control buttons:
    - restart backend
    - restart frontend
    - restart entire node
  - Display service state:
    - running
    - stopped
    - failed

  * Comments, descriptions, rules:
    - Service actions should call backend endpoints that interact with systemd.
    - Prevent accidental repeated restart actions.
    - Clearly show confirmation feedback after service operations.


[TODO] Task 086: Implement status refresh and live updates
  - Add periodic polling or websocket updates for:
    - lifecycle state
    - runtime health
    - onboarding progress
    - service status
  - Update UI components dynamically when state changes.

  * Comments, descriptions, rules:
    - Refresh interval should be reasonable (e.g., 5–10 seconds).
    - Avoid heavy polling that could overload the node.
    - UI should degrade gracefully if backend endpoints temporarily fail.


[TODO] Task 087: Implement responsive layout improvements
  - Ensure the dashboard layout works on:
    - laptop screens
    - tablet-sized displays
    - narrow browser windows
  - Adjust card layout to stack vertically when screen width is limited.

  * Comments, descriptions, rules:
    - Avoid fixed widths that break on smaller displays.
    - Maintain visual hierarchy even when stacked.
    - Keep the design consistent with the existing theme.


[TODO] Task 088: Improve visual consistency with shared theme styles
  - Standardize colors, badges, and typography across all cards.
  - Define reusable UI components:
    - status badge
    - health indicator
    - card header layout
  - Align the UI with the Synthia visual design.

  * Comments, descriptions, rules:
    - Avoid inline styling; use shared CSS variables or theme definitions.
    - Maintain dark-mode compatibility.
    - Keep UI minimal and readable.


[TODO] Task 089: Add UI diagnostics section for debugging
  - Create a hidden or expandable diagnostics panel.
  - Show:
    - current lifecycle state
    - API endpoints in use
    - last backend update timestamp
    - UI version
  - Allow copying diagnostic info.

  * Comments, descriptions, rules:
    - This helps debugging during development and support.
    - Diagnostics must never expose secrets or tokens.
    - Panel can be collapsible to avoid cluttering the main UI.


[TODO] Task 090: Create UI validation checklist for the dashboard
  - Verify that the dashboard correctly reflects:
    - unconfigured node
    - onboarding in progress
    - pending approval
    - trusted node
    - operational node
    - degraded node
  - Confirm that:
    - lifecycle colors update correctly
    - cards update after state transitions
    - service controls work as expected

  * Comments, descriptions, rules:
    - This checklist ensures UI accuracy during lifecycle transitions.
    - Validate behavior across browser refreshes and restarts.
    - This task finalizes the UI enhancement phase.
