Workflow skill: $synthia-workflow 
(/home/dan/.codex/skills/synthia-workflow/SKILL.md)

## Codex Execution Directive

You are acting as the Synthia development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

!!! IMPORTANT !!!!
Stop processing task when you reach the [STOP] flag!

Do not edit above this line:
#######################################################################

[WIP] Task 183: Select classification model
Choose the smallest available LLM for capability classification.

Priority order:
1. gpt-x.x-nano
2. gpt-x.x-mini

Fallback:
- smallest available GPT model

Store selected classification model.

[TODO] Task 184: Implement capability classification prompt
Create a structured prompt that asks the classification model to map capabilities. 

Capabilities to classify:

- reasoning
- vision
- image_generation
- audio_input
- audio_output
- realtime
- tool_calling
- structured_output
- long_context
- coding_strength
- speed_tier
- cost_tier
- recommended_for

The model must return JSON only.

[TODO] Task 185: Define controlled vocabulary for recommended_for
Use a fixed set of allowed categories:

recommended_for options:

- chat
- agents
- automation
- reasoning
- coding
- structured_extraction
- summarization
- classification
- vision_analysis
- image_generation
- speech_recognition
- speech_generation
- realtime_voice
- embeddings
- moderation

The classification prompt must instruct the model to use only these values.

[TODO] Task 186: Run capability classification
For each filtered model as a batch.

- Send classification request to the chosen nano/mini model.
- Parse JSON result.
- Validate schema.

Reject invalid outputs.

[TODO] Task 187: Persist model capability catalog
Store classification results locally:

provider_model_capabilities.json

Fields:

model_id
family
reasoning
vision
image_generation
audio_input
audio_output
realtime
tool_calling
structured_output
long_context
coding_strength
speed_tier
cost_tier
recommended_for

Store timestamp and classification_model used.

[STOP]

[TODO] Task 188: Add capability mapping UI
Display discovered models to the user.

UI elements:

Model card with:

- model name
- capability badges
- speed tier
- cost tier
- recommended tasks

Allow the user to enable/disable models.

[TODO] Task 189: Persist selected models
Store user-selected models:

provider_enabled_models.json

Fields:

model_id
enabled
selected_at

Only enabled models should influence node capabilities.

[TODO] Task 190: Derive node capabilities from selected models
Create a resolver that maps enabled models to node capabilities.

Example mappings:

reasoning → reasoning tasks
coding_strength high/medium → coding tasks
vision → image analysis
image_generation → image generation tasks
audio_input → speech-to-text
audio_output → text-to-speech
realtime → realtime voice interaction
structured_output → structured extraction
long_context → long-document processing

Combine capabilities across all enabled models.

[TODO] Task 191: Build node task capability list
From resolved model capabilities generate node task families:

Examples:

task.classification
task.summarization
task.reasoning
task.coding
task.vision_analysis
task.image_generation
task.speech_to_text
task.text_to_speech
task.realtime_voice
task.embedding_generation
task.moderation

Ensure tasks are declared only when supported by selected models.

[TODO] Task 192: Update node capability declaration manifest
Modify node declaration builder to include resolved task capabilities.

Declaration should include:

- node metadata
- declared task families
- provider metadata
- enabled models

Ensure compatibility with existing Core declaration schema.

[TODO] Task 193: Declare capabilities to Core
Send capability declaration to Core endpoint:

POST /api/system/nodes/capabilities/declaration

Include trust token header.

Handle responses:

- success
- rejected
- validation errors

Persist last successful declaration.

[TODO] Task 194: Add redeclaration trigger
Trigger capability redeclaration when:

- enabled models change
- provider configuration changes
- capability catalog refresh occurs

Avoid unnecessary redeclarations.

[TODO] Task 195: Add diagnostics panel
Create an admin diagnostics section showing:

- discovered models
- enabled models
- capability catalog
- classification model used
- last declaration payload
- last declaration result

Hide from non-admin users.

[TODO] Task 196: Add manual refresh controls
Add admin actions:

- refresh model discovery
- rerun capability classification
- rebuild node capabilities
- redeclare capabilities to Core

Avoid full node restart.

[TODO] Task 197: Add tests for model filtering
Test filtering logic for:

- valid model families
- excluded preview models
- excluded snapshot models
- duplicate removal

Ensure deterministic filtering.

[TODO] Task 198: Add tests for capability classification schema
Test:

- valid JSON outputs
- invalid schema rejection
- controlled vocabulary enforcement
- recommended_for validation

Ensure classification results remain stable.
