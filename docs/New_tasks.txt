Workflow skill: $synthia-workflow 
(/home/dan/.codex/skills/synthia-workflow/SKILL.md)

## Codex Execution Directive

You are acting as the Synthia development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

!!! IMPORTANT !!!!
Stop processing task when you reach the [STOP] flag!

Do not edit above this line:
#######################################################################

[WIP] Task 216: Add declaration integration tests
Create a graph definition file:

capabilities/task_graph.json

Each node represents a task capability and defines feature dependencies.

Graph rule format:

{
  "task.reasoning": {
    "all_of": ["reasoning"]
  }
}

Define the following task capability nodes:

Core intelligence
- task.chat
- task.reasoning
- task.classification
- task.summarization
- task.information_extraction
- task.structured_extraction
- task.translation
- task.sentiment_analysis

Automation / orchestration
- task.task_planning
- task.workflow_reasoning
- task.tool_usage
- task.automation_command
- task.environment_control

Coding
- task.code_generation
- task.code_review
- task.code_debugging
- task.code_explanation

Vision
- task.vision_analysis
- task.object_detection
- task.image_description
- task.document_ocr

Images
- task.image_generation
- task.image_editing
- task.image_variation

Speech
- task.speech_to_text
- task.text_to_speech
- task.voice_conversation
- task.audio_analysis

Realtime
- task.realtime_voice
- task.streaming_response
- task.low_latency_interaction

Knowledge
- task.embedding_generation
- task.semantic_search
- task.document_indexing
- task.knowledge_retrieval

Safety
- task.moderation
- task.policy_check

Create resolver module:

runtime/capability_resolver.py

Resolver logic:

1. collect enabled models
2. union all feature flags
3. evaluate graph rules
4. return enabled tasks

Pseudo logic:

features_union = union(model_features)

for each task_node:
    if dependency rules satisfied:
        enable task

Create utility that merges feature flags from all enabled models.

Rules:

- feature is enabled if ANY enabled model supports it
- features stored as boolean set

Using the capability graph resolver:

Input:
enabled_models

Output:
enabled_task_capabilities

Store result as:

runtime/node_capabilities.json

Modify declaration payload generator.

Instead of declaring raw model capabilities:

Declare resolved task capabilities only.

Example:

declared_task_families:

- task.reasoning
- task.classification
- task.summarization
- task.vision_analysis
- task.speech_to_text

Add provider intelligence metadata to capability declaration payload.

Fields:

provider
enabled_models
feature_union
resolved_tasks
capability_graph_version

This fixes the current Core rejection issue.

Modify setup page to display two sections:

Model Features
- unioned features across enabled models

Resolved Node Tasks
- tasks enabled via capability graph

Add UI card:

Resolved Node Capabilities

Show:

Enabled Models
Feature Union
Resolved Tasks
Classifier Model Used

Create admin diagnostics view.

Display:

discovered models
feature catalog
capability graph
enabled models
resolved tasks
last declaration payload

Allow manual actions:

refresh provider models
re-run classification
recompute capability graph
redeclare capabilities to Core

Test classification JSON:

- unknown feature keys rejected
- required schema enforced
- boolean fields validated

Test graph resolver:

- single model
- multiple models
- missing features
- partial graph matches

Verify:

node capabilities resolve correctly
payload sent to Core matches resolved tasks
provider_intelligence included
