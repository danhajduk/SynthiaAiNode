Workflow skill: $synthia-workflow
(/home/dan/.codex/skills/synthia-workflow/SKILL.md)
## Codex Execution Directive

You are acting as the Synthia development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

Do not edit above this line !

################################################################################
Task list
################################################################################

[TODO] Task 004: Define bootstrap message contract
  - Describe the structure of the Core bootstrap advertisement.
  - Include the fields the node needs to continue registration:
    - core_id
    - core_api_url
    - registration endpoint/path
    - protocol version
    - registration_open flag
  - Document validation expectations for received bootstrap messages.

  * Comments / descriptions / rules:
    - Keep the bootstrap payload minimal and non-sensitive.
    - This contract is for discovery only, not trust or policy delivery.
    - The node should ignore malformed, stale, or incomplete bootstrap payloads.


[TODO] Task 005: Define node registration handshake
  - Document how the node registers after receiving bootstrap data.
  - Define the registration payload fields:
    - node_name
    - node_type = ai-node
    - node software version
    - protocol version
    - optional hostname
  - Describe Core-side pending registration state.

  * Comments / descriptions / rules:
    - Registration must happen over API, not MQTT.
    - The node introduces itself as an AI Node class.
    - Core should already know what an AI Node class is.


[TODO] Task 006: Define operator approval through Core UI
  - Add the approval step into the registration handshake.
  - Describe the “pending approval” state on Core.
  - Define the approval flow:
    - Core generates approval session/token/link
    - operator authenticates in Core UI
    - operator approves or rejects the node
  - Document user-facing behavior for headless node setup.

  * Comments / descriptions / rules:
    - Nodes must not auto-join without operator approval.
    - Approval must happen in Core UI after login/authentication.
    - Rejected nodes must not receive trust tokens or operational credentials.


[TODO] Task 007: Define 2.1 initial trust activation payload
  - Document what Core returns after operator approval.
  - Define the trust activation response contents:
    - node_id
    - accepted node name
    - node type
    - paired core_id
    - node trust token
    - initial baseline policy
    - operational MQTT identity/token
    - operational MQTT endpoint details
  - Describe how the node stores the trust package.

  * Comments / descriptions / rules:
    - Call this “initial baseline policy,” not full policy.
    - This phase activates trust; it does not yet declare full node capability.
    - The node must persist the trust package for reboot/reconnect.


[TODO] Task 008: Define local persisted node state
  - Specify what the node must store locally after successful trust activation.
  - Include:
    - node_id
    - node_name
    - node_type
    - paired core_id
    - core API endpoint
    - trust token
    - baseline policy version
    - operational MQTT identity/token
    - bootstrap MQTT host
    - registration timestamp
  - Define persistence expectations across restart and upgrade.

  * Comments / descriptions / rules:
    - Persist only what is needed for stable reconnect and local enforcement.
    - Stored secrets/tokens must be treated as sensitive local state.
    - The node should not require re-onboarding on every reboot.


[TODO] Task 009: Define node lifecycle states
  - Create a lifecycle/state model for the node.
  - Include at least:
    - unconfigured
    - bootstrap_connecting
    - bootstrap_connected
    - core_discovered
    - registration_pending
    - pending_approval
    - trusted
    - capability_setup_pending
    - operational
    - degraded
  - Add transition notes between states.

  * Comments / descriptions / rules:
    - State names should be human-readable and stable enough for UI/telemetry.
    - Keep the state model simple and useful.
    - Do not mix future AI workload states into bootstrap/trust states yet.


[TODO] Task 010: Define post-bootstrap operational channel separation
  - Document the transition from anonymous bootstrap to trusted operational communications.
  - Define which channels are used for:
    - bootstrap discovery
    - trusted MQTT operations
    - control/API operations
    - telemetry
  - Add a clean explanation of why bootstrap and operational traffic must remain separate.

  * Comments / descriptions / rules:
    - Bootstrap stays narrow and anonymous.
    - Trusted MQTT must use Core-issued operational identity/token.
    - API remains the primary deterministic control path.


[TODO] Task 011: Define node capability declaration model (Phase 2.2)
  - Create the concept document for node capability declaration.
  - Define that 2.2 happens only after trust activation.
  - Explain the purpose:
    - Core does not know actual node capabilities until the node declares them.
  - Describe capability declaration as a formal manifest submitted by the node.

  * Comments / descriptions / rules:
    - Capability declaration is not prompt registration and not AI execution.
    - Core stores declared capabilities; Core does not invent them.
    - Declared capability does not automatically mean enabled by policy.


[TODO] Task 012: Define user-driven provider/service selection in 2.2
  - Document the setup flow where the operator chooses which AI providers/services should be enabled on the node.
  - Define the UX for provider selection.
  - For now, include OpenAI as the only real option.
  - Leave placeholders for future providers.

  * Comments / descriptions / rules:
    - Ask what should be enabled on this node, not only what the node could theoretically support.
    - Separate “supported” from “enabled.”
    - Keep provider secrets local to the node whenever possible.


[TODO] Task 013: Define capability groups for the declaration manifest
  - Split node capability declaration into clear groups.
  - Define at least:
    - functional AI task families
    - provider support
    - node features
    - environment/resource hints
  - Document each group and what belongs there.

  * Comments / descriptions / rules:
    - Keep environment/resource hints light at this phase.
    - Capability groups should be stable enough to support future policy refinement.
    - Avoid overloading the initial manifest with runtime-manager specifics too early.


[TODO] Task 014: Define initial functional task families
  - Document the first AI task families the node may declare.
  - Include:
    - text classification
    - email classification
    - image classification
    - image generation
  - Mark them as declared capability targets, not necessarily all active in implementation yet.

  * Comments / descriptions / rules:
    - These task families should align with future gateway API design.
    - Keep naming consistent across docs.
    - This is architecture-first, not implementation-first.


[TODO] Task 015: Define provider capability and enabled-provider model
  - Document the difference between:
    - providers supported by the node
    - providers enabled by the operator
  - For now, define OpenAI as the only implemented provider path.
  - Add placeholders for future providers/local services.

  * Comments / descriptions / rules:
    - Core should store both what is supported and what is enabled.
    - Supported != approved.
    - Enabled != unlimited use.


[TODO] Task 016: Define node feature flags/features manifest
  - Document node-level features that can be declared independently from task families.
  - Examples:
    - policy enforcement
    - telemetry support
    - prompt registration support
    - prompt governance support
    - operational MQTT support
    - future local runtime controller support
  - Define how these are represented conceptually.

  * Comments / descriptions / rules:
    - This helps Core understand the maturity/features of a node.
    - Keep it extensible without becoming vague.
    - Separate execution features from management features.


[TODO] Task 017: Define baseline environment/resource hints
  - Document what light system/environment traits the node may report in 2.2.
  - Include examples like:
    - hostname
    - OS/platform
    - memory class
    - GPU present yes/no
  - Clarify that this is not full telemetry or hardware inventory.

  * Comments / descriptions / rules:
    - Keep this intentionally lightweight in early phases.
    - These hints are for later policy/routing context, not detailed monitoring.
    - Avoid promising resource-based scheduling yet.


[TODO] Task 018: Define capability submission and Core acceptance flow
  - Document how the node submits its capability declaration to Core.
  - Define Core behavior:
    - validate manifest format
    - store capability record
    - acknowledge accepted profile
  - Describe the distinction between accepted capability record and later policy enablement.

  * Comments / descriptions / rules:
    - Core should validate structure, not blindly trust everything semantically.
    - Capability acceptance does not equal feature enablement.
    - Keep this API-based and deterministic.


[TODO] Task 019: Define initial baseline policy structure
  - Create the architecture section for initial baseline policy.
  - Define what baseline policy can include before full capability-aware policy exists.
  - Include:
    - policy version
    - issue timestamp
    - refresh expectations
    - generic node-class rules
    - telemetry expectations
    - feature gating defaults
  - Clarify what is intentionally excluded at this stage.

  * Comments / descriptions / rules:
    - Do not make baseline policy depend on undeclared node capabilities.
    - This is the minimum viable governance package.
    - Keep room for later capability-shaped policy refinement.


[TODO] Task 020: Define trusted MQTT operational identity model
  - Document how Core provisions trusted MQTT identity/token for the node after approval.
  - Define what the node receives:
    - MQTT username/identity
    - MQTT token/password
    - trusted MQTT host/port
    - allowed topic scope/namespace
  - Explain how this differs from bootstrap MQTT access.

  * Comments / descriptions / rules:
    - Bootstrap MQTT and trusted operational MQTT are separate concerns.
    - Operational MQTT must be authenticated and scoped.
    - Anonymous bootstrap must never become the general-purpose message path.


[TODO] Task 021: Define node reconnect and resync behavior
  - Document what the node should do on reboot, restart, or temporary Core outage.
  - Include behavior for:
    - restoring local identity
    - reconnecting to trusted MQTT
    - reusing stored baseline policy
    - falling back to bootstrap only if needed
  - Define degraded/stale states conceptually.

  * Comments / descriptions / rules:
    - The node should prefer its paired Core after initial onboarding.
    - Rebootstrap should be fallback behavior, not default behavior.
    - Keep reconnect logic deterministic and safe.


[TODO] Task 022: Define baseline status telemetry architecture
  - Document what status/telemetry the node should emit before AI execution exists.
  - Include:
    - bootstrap connected
    - pending approval
    - trusted
    - capability declared
    - degraded
    - disconnected
    - policy stale
  - Define intended transport(s) conceptually.

  * Comments / descriptions / rules:
    - This phase is about node operational telemetry, not AI workload telemetry.
    - Status messages should support future UI visibility and debugging.
    - Do not send telemetry on anonymous bootstrap.


[TODO] Task 023: Define security boundaries for bootstrap, trust, and operations
  - Create a dedicated security section in the architecture doc.
  - Document:
    - anonymous bootstrap limitations
    - operator approval requirement
    - token issuance boundaries
    - secrets stored locally on node
    - API vs MQTT trust boundaries
  - Add a “must not” list for unsafe shortcuts.

  * Comments / descriptions / rules:
    - No secrets in bootstrap payloads.
    - No node publishing on bootstrap channel.
    - No automatic trust without UI/operator approval.
    - Keep this section blunt and explicit.


[TODO] Task 024: Define the future execution gateway placeholder
  - Add an architecture placeholder for the future AI execution gateway.
  - Document that later phases will expose normalized AI request APIs for:
    - text classification
    - email classification
    - image classification
    - image generation
  - Explain why the placeholder exists even though execution is not yet implemented.

  * Comments / descriptions / rules:
    - This keeps current architecture aligned with future build direction.
    - Avoid implementation detail here.
    - Make it clear this phase does not yet run AI workloads.


[TODO] Task 025: Define future local runtime manager placeholder
  - Add an architecture placeholder for later local runtime management support.
  - Mention the future concepts:
    - local runtime controller
    - desired state
    - observed state
    - managed local providers
  - Keep it clearly marked as future work.

  * Comments / descriptions / rules:
    - This placeholder exists to avoid painting the architecture into a corner.
    - Do not design the full runtime controller in this task.
    - Keep Phase 1/2 architecture clean and focused.


[TODO] Task 026: Create a phased architecture roadmap section
  - Add a final roadmap section to the architecture document.
  - Define the phases in order:
    - 001 bootstrap and registration
    - 002 trust activation and capability declaration
    - future prompt governance
    - future OpenAI execution
    - future multi-provider support
    - future local runtime support
  - Summarize what each phase unlocks.

  * Comments / descriptions / rules:
    - Keep the roadmap high-level and aligned with the architecture decisions above.
    - Make each phase build logically on the previous one.
    - Avoid mixing future implementation detail into early phases.
